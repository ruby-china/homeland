<% if @topics.present? %>
<div class="box clearfix">
  <h2 class="title"><%= t("topics.node_recent_topics") %></h2>
  <ul>
  <% for topic in @topics do %>
    <li><%= link_to(truncate(topic.title, length: 30), topic_path(topic), title: topic.title) %></li>
  <% end %>
  </ul>
</div>
<% end %>
