<div class="suggest_topics box">
  <h2 class="title"><%= t("common.highlight_topic")%></h2>
  <ul>
    <% @suggest_topics.each do |item| %>
      <li><%= user_avatar_tag(item.user, :small) %> <%= link_to(item.title, topic_path(item))%></li>
    <% end %>
  </ul>
</div>