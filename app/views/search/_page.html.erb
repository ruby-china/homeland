<div class="page">
  <div class="title"><%= link_to(search_result_highlight(hit, :title), page_path(page.slug)) %></div>
  <div class="url"><%= link_to(page_url(page.slug), page_path(page.slug)) %></div>
  <div class="info"><%= t("common.published_at")%> <%= timeago(page.updated_at) %></div>
  <div class="summary">
    <%= search_result_highlight(hit, :body) %>
  </div>
</div>