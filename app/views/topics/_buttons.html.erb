<div class="opts">
  <% if !turbolinks_ios? %>
    <%= likeable_tag(@topic) %>
    <%= topic_follow_tag(@topic) %>
    <%= topic_favorite_tag(@topic) %>
  <% end %>

  <div class="pull-right">
    <div class="dropdown">
      <a href="#" data-toggle="dropdown">
        <%= icon_tag("ellipsis-v") %>
      </a>
      <%= render "menu" %>
    </div>
  </div>
</div>
