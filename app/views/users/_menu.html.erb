<%= render_list :class => "tabs" do |li|
  li << link_to(t("users.menu.profile"), user_path(@user.login))
  li << link_to(t("users.menu.topics"), topics_user_path(@user.login))
  li << link_to(t("users.menu.favorites"), favorites_user_path(@user.login))
end %>
