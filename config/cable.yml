# Action Cable uses Redis to administer connections, channels, and sending/receiving messages over the WebSocket.
production:
  url: redis://localhost:6379/1

development:
  adapter: postgresql
  url: postgres://localhost/ruby-china-dev

test:
  url: redis://localhost:6379/3
